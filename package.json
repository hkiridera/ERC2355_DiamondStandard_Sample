{
  "name": "diamond-1",
  "version": "1.4.0",
  "description": "Gas-optimized diamond for adding/replacing/removing functions.",
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --write contracts/**/*.sol",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf build && npx truffle migrate"
  },
  "author": "",
  "license": "ISC",
  "prettier": {
    "printWidth": 150
  },
  "dependencies": {
    "ethers": "^5.0.23",
    "truffle": "^5.1.61",
    "web3": "^1.3.1",
    "web3-eth-contract": "^1.3.1"
  },
  "devDependencies": {
    "@codechecks/client": "^0.1.10",
    "eth-gas-reporter": "^0.2.20",
    "prettier": "^2.2.1",
    "prettier-plugin-solidity": "^1.0.0-beta.2",
    "standard": "^14.3.4",
    "typescript": "^4.1.3"
  }
}
